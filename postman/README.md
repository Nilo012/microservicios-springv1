# Postman

Import the collection below into Postman:

- [teaching-project-phase1.postman_collection.json](/Users/georgemaxi/Desktop/ms-project/postman/teaching-project-phase1.postman_collection.json)

Default variables already point to the local services:

- `productServiceUrl = http://localhost:8081`
- `orderServiceUrl = http://localhost:8082`
- `productId = 1`
- `orderId = 1`

Suggested flow:

1. Run `Create Product`
2. Run `Get Product By Id`
3. Run `Create Order`
4. Run `Get Order By Id`
